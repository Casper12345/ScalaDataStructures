package mergeSort
/*

object MergeSort {
  def apply() = new MergeSort
}

class MergeSort {

  def sort[A <: Ordered[A]](lst: List[A]): List[A] = {
    val m = lst.size / 2

    val split = lst.splitAt(m)

    sort(split._2)
    sort(split._1)


    Nil
  }

  def merge[A <: Ordered[A]](lst1: List[A], lst2: List[A], lst: List[A]): List[A] = {
    def looper(lst: List[A], i: Int = lst.size): A = i match {
      case 0 =>

    }

  }


}

object Main extends App {
  val m = MergeSort.apply()
  m.sort(List(1, 2, 3, 4, 5, 6))

}

*/